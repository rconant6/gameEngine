pub const Alignment = struct {
    pub const Horizontal = enum {
        start,
        center,
        end,
        stretch,
    };
    pub const Vertical = enum {
        start,
        center,
        end,
        stretch,
    };
};
